import { COMMENTS } from '../shared/comments';
import * as ActionTypes from './ActionTypes';

export const Rcomments = (state = COMMENTS, action) => {
    switch (action.type) {

        default:
          return state;
      }
};